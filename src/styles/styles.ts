import styled from '@emotion/styled'

export const AuthComponent = styled.div({
  marginTop: 20,
  backgroundColor: '#',
  display: 'flex',
  flexDirection: 'column',
  flex: 3,
  alignItems: 'center',
  justifyContent: 'center',
})

export const HeaderComponent = styled.div({
  backgroundColor: '#fff',
  display: 'flex',
  flexDirection: 'row',
  flex: 1,
  justifyContent: 'center',
  alignItems: 'center',

})

export const BodyComponent = styled.div({
  marginTop: 20,
  backgroundColor: '#',
  display: 'flex',
  flexDirection: 'column',
  flex: 3,
})

export const FooterComponent = styled.div({
  backgroundColor: '#fff',
  display: 'flex',
  flexDirection: 'row',
  flex: 1,
})

export const TitleComponent = styled.header({
  color: '#000000',
  fontSize: 48,
  fontFamily: 'unset'
})